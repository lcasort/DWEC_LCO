let suma = (a,b) => a+b; 
console.log(suma(2,3));

let sinParams = () => {console.log('Hola.'); return 'Adios.';}
console.log(sinParams());

// (p1,p2,...) => sentencia
// (p1,p2,...) => {sentencias}
// () => sentencia
// () => {sentencias}
// p => sentencia
// p => {sentencias}